class Solution {
public:
    string reverseString(string s) {
        string result;
        int len = s.length();
        int i = len-1;
        if(len==0)
        {
            return result;
        }
        while(i>=0)
        {
            result += s[i];
            i--;
        }
        return result;
    }
};
